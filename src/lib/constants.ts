export const TAB_VALUES = {
  LOGIN: 'login',
  REGISTER: 'register',
} as const

export const ROUTES = {
  authEntry: '/',
  voiceSets: '/voice-sets',
  voiceSet: '/voice-sets/:voiceSetId',
} as const
